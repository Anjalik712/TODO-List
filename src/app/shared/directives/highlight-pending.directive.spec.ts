import { HighlightPendingDirective } from './highlight-pending.directive';

describe('HighlightPendingDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightPendingDirective();
    expect(directive).toBeTruthy();
  });
});
